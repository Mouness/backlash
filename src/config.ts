// Feature Flags

// Toggle this to enable/disable mock data application-wide
// In a real app, this could be an environment variable: import.meta.env.VITE_ENABLE_MOCKS
export const ENABLE_MOCKS = false;
